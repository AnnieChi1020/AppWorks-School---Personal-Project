# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
"on": [pull_request]
env:
  REACT_APP_FIREBASE: ${{ secrets.FIREBASE }}
  REACT_APP_GOOGLEMAP: ${{ secrets.GOOGLEMAP }}
  REACT_APP_AUTOCOMPLETE: ${{ secrets.AUTOCOMPLETE }}
jobs:
  build_and_preview:
    if: "${{ github.event.pull_request.head.repo.full_name == github.repository }}"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install && CI=true npm test && CI=true npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_VOLUNTEER_C29D0 }}"
          projectId: volunteer-c29d0
